@model Hybriotheca.Web.Models.Entities.LoanViewModel

@{
    ViewData["Title"] = "Details";
    Layout = "_Layout-BACKUP";
}

<h1>Details</h1>

<div>
    <h4>Loan</h4>
    <hr />
    <partial name="_Display" />
</div>
<div>
    @if (!Model.IsReturned)
    {
        <a asp-action="ReturnBook" asp-route-loanId="@Model?.Id" class="btn btn-primary">Return Book</a>
    }
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a>
    <a asp-action="Index">Back to List</a>
</div>
